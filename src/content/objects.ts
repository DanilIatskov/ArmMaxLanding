/**
 * Объекты. Карточки заполняет администратор.
 *
 * TODO (важно для позиционирования): под новое позиционирование нужны крупные
 * объекты — название, город, год, объём работ, что именно делали, фото
 * с разрешением на публикацию. Объекты с кровельного лендинга (коттеджи
 * 320–380 м², 2020 год) сюда не годятся: они работают против задачи
 * «объекты от 300 млн ₽, а не дачные постройки».
 */

export type BuildObject = {
  slug: string
  title: string
  city: string
  /** Тип объекта: промышленный, общественный, жилой, инфраструктурный. */
  type: string
  year: number
  /** Объём работ — что делали: «генподряд», «проектирование + строительство». */
  scope: string
  area?: string
  image: string
  /** Метка на карте: [широта, долгота]. */
  geo?: [number, number]
}

export const objects: BuildObject[] = []
