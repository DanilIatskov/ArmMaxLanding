# Сайт ООО «АРММАКС-СТРОЙ»

Корпоративный сайт строительной компании из Новосибирска. Главная задача —
чтобы при поиске «АрммаксСтрой» помощник руководителя или служба безопасности
заказчика сразу находили официальный сайт с реквизитами, объектами и контактами,
а не однофамильцев.

## Стек

| Что | Чем | Почему |
| --- | --- | --- |
| Фреймворк | Next.js 16, App Router | Многостраничник со статической генерацией: под Яндекс нужна отдельная страница на каждую услугу |
| Язык | TypeScript | |
| Стили | Tailwind CSS 4 | Токены заданы в CSS, без JS-конфига — см. `src/app/globals.css` |
| Шрифты | Roboto Condensed + Manrope | Roboto Condensed — все заголовки, логотип, навигация, первый экран, крупные цифры. Manrope — основной текст и ссылки |
| Хостинг | Node.js в РФ | 152-ФЗ: персональные данные из формы не должны покидать периметр РФ |

## Команды

```bash
npm install
npm run dev        # разработка, http://localhost:3000
npm run build      # продакшен-сборка
npm run start      # запуск собранного
npm run typecheck  # tsc --noEmit
npm run lint       # eslint

npm run images     # PNG-исходники из images/ → JPEG в src/assets/
npm run logo       # контуры логотипа из images/logo.svg → logo-paths.ts
npm run favicon    # фавикон из images/logo.svg
npm run og         # картинка превью для ссылок (после npm run build)
```

## Структура

```
src/
  app/                  маршруты (App Router)
    api/lead/           приём заявок с формы
    uslugi/[slug]/      страница услуги, генерируется из src/content/services.ts
    sitemap.ts          карта сайта
    robots.ts           robots.txt
  components/           UI: шапка, подвал, форма, логотип, микроразметка
  content/              ВЕСЬ ТЕКСТ И ФАКТЫ КОМПАНИИ
```

### Где править контент

Компоненты не хранят текст. Правки — только в `src/content/`:

- `company.ts` — реквизиты, контакты, награды, принципы
- `services.ts` — пять услуг, тексты подстраниц и их SEO-заголовки
- `process.ts` — этапы «как мы работаем»
- `objects.ts` — карточки объектов
- `sectors.ts` — типы объектов для тёмной сетки и лента бегущей строки
- `nav.ts` — пункты меню

Цифры блока «АРММАКС-СТРОЙ в цифрах» лежат в `src/components/GlanceGrid.tsx`:
там же задана раскладка шахматной сетки, и цифра неотделима от своей ячейки.

Слой контента типизирован и изолирован. Когда появится админка, его заменяет
источник данных из CMS — компоненты трогать не придётся.

## Что блокирует запуск

Поля с `null` в `src/content/company.ts` выводятся на сайте плашкой
«уточняется» — незаполненными их не выкатить незаметно.

1. **Реквизиты — закрыто.** Заполнены из учётной карточки организации:
   ИНН 5410069125, КПП, ОГРН, ОКВЭД, юрадрес, банк, директор Бахтеев
   Станислав Борисович. ИНН сходится с номером СРО. Осталась только дата
   регистрации — в карточке её нет.
2. **СРО.** Номер есть — № СРО-5410069125-20260306-1434. Нужна выписка
   из реестра. СРО на проектирование с сайта убрано по просьбе заказчика —
   членства нет, и заявлять его нельзя. Проектные работы при этом остались:
   они идут через проектную организацию.
3. **Упоминание чужих заказчиков.** В `src/content/projects.ts` — объекты
   НОВАТЭКа, Газпрома и Транснефти. Два условия, без которых это не выкатывать:

   - **Работы велись не от ООО «АРММАКС-СТРОЙ», а в составе ГК «Армакс».**
     Так сказал заказчик. В блоке это написано прямо, и убирать формулировку
     нельзя. Сайт открывает служба безопасности партнёра; если подать чужие
     по документам объекты как свои, проверка не найдёт подтверждения —
     и доверие потеряно ко всему сайту, а не к одной строке.
   - **Нужно письменное согласие на упоминание.** Названия компаний —
     их товарные знаки, а в договорах с компаниями такого масштаба обычно
     есть пункт о неразглашении и о согласовании публикаций. Отдельно
     и строго: **логотипы заказчиков без письменного разрешения не ставим.**
     Назвать компанию словом в перечне работ и разместить её фирменный знак —
     это юридически разные вещи, и второе рискованнее.

4. **Объекты — сейчас там ПРИМЕРЫ.** В `src/content/objects.ts` лежат
   выдуманные карточки: они показывают раздел в работе и обязаны уехать
   до запуска. Сайт делается для проверки компании, и службе безопасности
   достаточно не найти подтверждения одному объекту, чтобы снять доверие
   со всего сайта. На сайте это ничем не помечено — предупреждение только
   в комментарии к данным, так что проверять перед запуском нужно осознанно.
   Нужны: название, город, тип, год, объём работ, площадь и фото
   с разрешением на публикацию. Коттеджи 320–380 м² с кровельного лендинга
   сюда не годятся.
5. **Типы объектов.** Список в `src/content/sectors.ts` выведен из
   позиционирования, а не из подтверждённого перечня. Заказчику нужно вычеркнуть
   лишнее и дописать своё: под эти же формулировки пойдут посадочные запросы.
6. **Домен.** `armaxstroy.ru` занят кровельным лендингом. Свободность вариантов
   (`armmaks-stroy.ru`, `armmax-stroy.ru`, `арммакс-строй.рф`) проверять
   на reg.ru или nic.ru. Оформлять на компанию, доступы — у заказчика.
7. **Почта.** `armaxstroy@mail.ru` на корпоративном сайте читается как минус.
   Лучше `info@` на своём домене.
8. **CRM.** Заказчик внедряет, какую — не сказал. Пока заявка пишется в лог
   сервера; доставка включается в `src/app/api/lead/route.ts`.
9. **Фамилия генерального директора.** Заказчик назвал только имя и отчество —
   Станислав Борисович. На странице, которую открывает служба безопасности,
   половина имени выглядит хуже, чем честное «уточняется».

## Переменные окружения

Смотри `.env.example`. Без `LEAD_EMAIL_TO` заявка не теряется, но и никуда
не уходит — только пишется в лог сервера с пометкой.

## SEO и подключения после запуска

- [ ] Яндекс Вебмастер: подтвердить права, положить код в `site.yandexVerification`
- [ ] Яндекс Метрика: номер счётчика в `NEXT_PUBLIC_YANDEX_METRIKA_ID`
- [ ] Карточки в Яндекс Бизнесе, 2ГИС и Google Maps — с тем же названием,
      адресом, телефоном и ссылкой на сайт, что на сайте. Именно они
      показываются первыми при поиске компании
- [ ] Яндекс.Карты: ключ в `NEXT_PUBLIC_YANDEX_MAPS_API_KEY`, карта офиса
      и метки объектов
- [x] Уведомление о cookie и страница политики cookie
- [x] `sitemap.xml`, `robots.txt`, микроразметка организации (schema.org)
- [x] Отдельная страница под каждую услугу

Микроразметка в `src/components/JsonLd.tsx` намеренно набита
идентифицирующими данными: задача — развести компанию с однофамильцами
(«Армакс» в Курске, armax.group, ООО «Армакс Строй» в Омске, дорожная Armax
и фирма по утилизации).

## Cookie и согласие

Уведомление о cookie — `components/CookieNotice.tsx`, решение хранится
в localStorage через `lib/cookie-consent.ts`.

Кнопки две, и это не формальность: баннер с одним «Принимаю» выбора
не предлагает, а значит и согласием не является. «Только нужные» оставляет
сайт работать и не пускает аналитику.

**Яндекс.Метрика подключается только после согласия.** Без этого баннер был бы
бутафорией: кнопка ни на что не влияла бы, а счётчик грузился бы всё равно.
Условий у счётчика два — номер в окружении и согласие.

Выбор читается через `useSyncExternalStore`, а не чтением в эффекте: хранилище
внешнее по отношению к React, и эффект сначала отрисовал бы состояние
по умолчанию. На сервере ответа нет вовсе, поэтому там отдельное значение
`unknown` — иначе баннер моргал бы у тех, кто уже выбрал.

Каждое обращение к localStorage обёрнуто: в приватном окне и при запрете
данных сайта оно бросает исключение. Не прочитали — считаем, что согласия нет.
Молчание не согласие.

Текст политики cookie типовой, как и политика по персональным данным: перед
запуском обе должен посмотреть юрист заказчика.

## Логотип и фавикон

Логотип заказчика — гранёный объёмный знак «М» и надпись в две строки.
Вектор лежит в `images/logo.svg`, растр для аватарок и печати — в
`images/logo.png`. Файлы прислал заказчик; перерисованный вручную
знак из первой версии сайта убран.

Надпись кириллическая: **АРММАКС** крупно и **СТРОЙ** разрядкой под ней.
Первые версии файла приходили латиницей (ARMMAX STROY), поэтому при смене
знака стоит смотреть не только на размер файла: контуры знака у всех версий
совпадают побайтово, а отличается одна только надпись.

### Логотип в вёрстке

`src/components/Logo.tsx` — вектор целиком в разметке, не картинкой: его надо
перекрашивать под фон, а `<img>` так не умеет.

Сами контуры лежат отдельно, в `src/components/logo-paths.ts`, и переносятся
туда из `images/logo.svg` командой `npm run logo`. Руками этот файл не правят.
Знак уже дважды менялся за время работы, а контуры занимают несколько
килобайт: одна потерянная цифра ломает фигуру незаметно. В `Logo.tsx` остаётся
только логика отрисовки.

После смены `images/logo.svg` прогоняем три команды: `npm run logo`,
`npm run favicon`, `npm run og` — компонент, фавикон и картинка превью.

Два состояния, потому что знак объёмный:

- `surface="dark"` — на тёмной шапке и в подвале идёт фирменный белый вариант
  с градиентами от белого к серо-синему, знак читается объёмным.
- `surface="light"` — на светлой шапке те же градиенты почти сливаются с фоном,
  поэтому знак берётся сплошным `currentColor`. Светотень уходит, но «М»
  не теряется: она задана силуэтом — две стойки и провал между ними.

Цвет сплошного варианта задаётся снаружи обычным `text-*`, отдельного файла
под каждую тему не нужно. Шапка переключает состояние тем же флагом, которым
переключает свой фон при прокрутке.

В сплошном варианте шесть граней слиты в один контур. Порознь каждая
сглаживается сама по себе, и на общих рёбрах остаются светлые волосяные швы:
два соседних контура закрывают пограничный пиксель наполовину каждый, а не
целиком. В градиентном варианте контуры остаются раздельными — там грани
и должны различаться по цвету.

Идентификаторы градиентов разведены по размеру логотипа (`sm`, `md`, `lg`):
на странице одновременно живут мобильный, десктопный и подвальный, а
повторяющийся `id` в документе — уже не тот `id`. Поэтому один размер
используется один раз.

### Фавикон

`src/app/icon.png` (256×256) и `src/app/apple-icon.png` (180×180) — Next
подхватывает их по имени и сам ставит теги в `<head>`. На иконке только знак
«М», без надписи: на вкладке 16×16 от слова осталась бы серая полоска.

Собирает `npm run favicon` из `images/logo.svg`. Знак берётся фирменный,
с градиентами, и кладётся на тёмный квадрат — тот же цвет, что у
`viewport.themeColor`. На светлом фоне эти градиенты почти слились бы с ним.
Скрипт сам считает габарит знака по его контурам и кадрирует с полями в 10%:
в исходнике вокруг знака пустое место под надпись, и без обрезки он ужался бы
до нескольких пикселей. Правки — в `scripts/make-favicon.mjs`, руками файлы
не трогаем.

### Превью ссылок (Open Graph)

Картинка — `public/og-preview-2.jpg`, 1200×630. Собирает `npm run og` из
вёрстки `images/og-preview.html`: тот же кадр и тот же набор строк, что на
первом экране. Логотип подставляется прямо из `images/logo.svg`, поэтому
превью не разъедется с сайтом при следующей смене знака. Скрипт снимает кадр
в headless Chrome и переводит в JPEG.

Шрифты берутся из сборки (`.next/static/media`), куда их кладёт `next/font`,
а не с Google Fonts. Сначала вёрстка тянула стили по сети — и когда запрос
не проходил, кадр молча снимался запасным шрифтом: он шире фирменного,
заголовок разъезжался на лишнюю строку, и заметить это можно было только
глазом на готовой картинке. Поэтому **перед `npm run og` нужен `npm run
build`** — без папки `.next` скрипт остановится с понятной ошибкой.

Готовность шрифта проверяется замером ширины строки, а не `document.fonts.check`:
тот отвечает «да», когда текст можно отрисовать хоть чем-нибудь, включая
запасной шрифт, и на эту проверку полагаться нельзя.

Номер в имени файла — не украшение. Соцсети кешируют превью по URL и надолго,
поэтому заметно изменённую картинку кладём под новым именем и правим ссылку
в `layout.tsx`; иначе в лентах ещё долго висит старая. Имя задаётся
в `scripts/make-og.mjs`.

Картинка объявлена руками в `src/app/layout.tsx`, а не через файловую
конвенцию `src/app/opengraph-image.jpg`. Причина: конвенция молча перебивает
`openGraph.images` целиком, а подпись к картинке читает только из
`opengraph-image.alt.txt` — этот файл разбирает webpack-загрузчик, тогда как
сборка идёт на Turbopack, и `og:image:alt` терялся.

Заголовок и описание превью каждая страница даёт свои: в корневом `layout.tsx`
у `openGraph` намеренно нет `title` и `description`, иначе они наследовались бы
на все страницы и ссылка на любую внутреннюю показывала бы заголовок главной.
По той же причине страницы не объявляют свой блок `openGraph`: он заменяет
родительский целиком — вместе с картинкой.

## Дизайн

Палитра выведена из первой версии логотипа (`#242d66`, `#2e3192`, `#4752a2`,
`#1b75bc`, `#00adef`) и референса ru-54.com (`#060d28`, `#017efa`). Новый
логотип пришёл ахроматичным — бело-серым, — поэтому синие остались за сайтом
и менять их не пришлось. Токены — в `@theme` в `src/app/globals.css`, там же
типографика и тени.

Пластика собрана по suffolk.com и skender.com (оба лежат в том же файле Figma
импортом страницы): острые углы, волосяные линии сетки, крупные плотные
заголовки, много намеренной пустоты.

Опорный мотив — **квадратный маркер** (`<Marker/>`) перед каждым надзаголовком.
У Suffolk он красный; у нас взят из логотипа, который сам собран из гранёных
квадратов, поэтому мотив свой, а не заимствованный.

Блоки, выдранные из референсов:

| Блок | Откуда | Файл |
| --- | --- | --- |
| Шапка с мега-меню | Figma 11:12 / 10:11926 | `components/Header.tsx` |
| Первый экран: слайдер с вкладками | Figma 4:4386 / 10:9523 / 10:9525 | `components/Hero.tsx` |
| Шахматная сетка цифр с пустыми ячейками | Suffolk | `components/GlanceGrid.tsx` |
| Вкладки «Как мы строим» | Suffolk | `components/SystemTabs.tsx` |
| Крупная навигация в футере | Suffolk | `components/Footer.tsx` |
| Тёмная сетка направлений с подчёркиваниями | Skender | `components/SectorsGrid.tsx` |
| Гармошка объектов | макет заказчика | `components/ProjectsAccordion.tsx` |

### Куда ведут нажатия

Два места на главной вели человека не туда, и оба правились по одной логике:
нажатие должно показывать результат, а не менять что-то за кадром.

**Вкладки «Как мы строим».** На широком экране список этапов стоит слева,
раскрытие — справа, всё видно сразу. На телефоне список уезжает наверх,
а панель встаёт под ним, и при нажатии на вкладку менялась только картинка
где-то внизу за краем экрана — казалось, что кнопка не работает. Теперь ниже
`lg` нажатие подтягивает описание этапа под шапку. Отступ даёт
`scroll-padding-top` у `html`, плавность гасится там же при
`prefers-reduced-motion`. Стрелки перелистывания внутри самой панели ничего
не прокручивают: они и так на экране.

**Строки «Что строим».** Вели на `/kontakty#zayavka` — уход на другую страницу
за формой, которая на этой же странице в двух экранах ниже. Ведут на `#zayavka`
рядом, а выбранный тип объекта подставляется в комментарий формы. Уже набранный
текст при этом не затирается. Когда под направления появятся посадочные
страницы, ссылки уедут на них — до тех пор форма ближе к делу, чем ничего.

### Первый экран

Заголовок снят с макета (`10:9523`): узкий гротеск в прописных, интерлиньяж
1.06, трекинг −0.007em. Начертания смешаны **в одном абзаце** — Bold
на утверждении («Строительство под ключ.»), Regular на пояснении. Граница
приходится на середину строки, поэтому абзац переносится естественно
и не разложен на строки заранее.

Из-за этого ушла прежняя анимация выезда строк из-под маски: она требует
заранее известных строк. Вместо неё — подъём заголовка и следом кнопок.

Кнопки — `10:9525`: высота 36px, 16px Condensed Bold, 32px сверху и 16px
между ними. Первая заливается акцентом, вторая тёмная.

**Слайдер** (`4:4386`): три слайда, вкладки по 278px с полосой прогресса
сверху, номер 22px, подпись 18px, неактивные на `opacity: .6`; справа
круглые стрелки. Полоса заполняется за время показа слайда.

Стрелки уменьшены вдвое против макета (40px вместо 74px): в оригинале они
стоят на фотографии и держат вес кадра, а на нашем спокойном фоне такой размер
перетягивал внимание с заголовка.

Слайды лежат в `src/content/slides.ts` — по одному сильному аргументу
из брифа на каждый: комплекс под ключ, выгодная точка подключения,
доведение до разрешения на ввод. Заголовок каждого разбит на `lead` (Bold)
и `rest` (Regular) — это и есть приём смешанных начертаний.

Автопрокрутка (7 секунд) останавливается на наведении и на фокусе внутри
слайдера, а при `prefers-reduced-motion` не запускается вовсе: иначе экран
уезжает из-под человека, который не успел дочитать. По той же причине
полоса прогресса там просто залита — она перестаёт быть таймером.

На узком экране видна только вкладка активного слайда: три по 278px
в 390px не помещаются, а показывать всегда первую — врать о том,
где человек находится.

### Сетка и отступы

Один отступ от края экрана на весь сайт: `--page-gutter` (20px / 56px от `lg`)
внутри `max-w-[1680px]`. Шапка, первый экран, все секции и подвал выстроены
по одной вертикали.

Блоки с фоном во всю ширину (колонки мега-меню) подводятся к той же вертикали
утилитами `.pl-edge` / `.pr-edge`: они считают отступ с учётом центрирования
в 1680px, поэтому фон доходит до края экрана, а текст стоит в общей колонке.

### Шапка

Снята с макета по параметрам (Figma `11:12` — закрытая, `10:11926` — раскрытая):

- логотип, навигация, кнопка связи и телефон в одну строку
- навигация — Roboto Condensed Bold 15px, прописные. Узкое начертание
  вмещает все пункты в строку без ужимания на 1024px
- пункты с раскрытием открывают **мега-меню во всю ширину**: слева серая
  колонка с заголовком, описанием и кнопкой, справа список с линейками 2px
- над тёмным первым экраном шапка прозрачная и светлая по тексту; при
  прокрутке, на внутренних страницах и при раскрытом меню — белая.
  Так же ведёт себя оригинал: на раскрытии шапка перекрашивается
- Escape закрывает раскрытие, иначе с клавиатуры из него не выйти

Верхней строки из макета здесь нет. На тёмном первом экране её линия читалась
как случайная и утяжеляла шапку, а единственная ссылка с неё («Реквизиты»)
открывается из раскрытия «О компании». По той же причине «Реквизиты»
не дублируются в основном меню.

Заголовки всего сайта набраны Roboto Condensed Bold — тем же семейством,
что шапка, логотип и первый экран. Правило стоит один раз на `h1…h4`
в `globals.css`, поэтому компоненты семейство не задают. Отрицательный трекинг
там почти снят: начертание и так узкое, и на минусе буквы слипались.

Основной текст и ссылки остались на Manrope — пара «узкий гротеск
на заголовках, обычный в тексте» держит иерархию без лишней пестроты.

Цифры в `GlanceGrid` — тот же Roboto Condensed крупным кеглем. В названии
компании стоит неразрывный дефис (U+2011): обычный рвал «АРММАКС-СТРОЙ»
по строкам.

Пустые ячейки в `GlanceGrid` — не недоделка. На них держится ритм блока;
если заполнить их «чтобы не пустовало», он превращается в обычную плитку.

Анимации: подъём заголовка первого экрана, появление блоков при скролле,
уезжающая стрелка в ссылках, отсчёт чисел в блоке «в цифрах» и гранёные плитки
(`FacetTile`). Всё отключается через `prefers-reduced-motion` — бриф просит
анимацию «умеренно, для акцентов».

`CountUp` держит в разметке итоговое значение, а на ноль подменяет его
в ref-колбэке, то есть до отрисовки кадра и внутри ещё прозрачного `Reveal`.
Поэтому поисковик и скринридер видят «25», а подмены не видно.

`FacetTile` — гранёная плитка из палитры логотипа. Сейчас **не используется**:
в блоке «Как мы строим» вместо неё кадры этапов. Компонент оставлен намеренно,
он пригодится там, где нужен фирменный узор без фотографии. Грани въезжают
волной по диагонали, потом медленно дышат вразнобой; дыхание стартует **после**
въезда — при двух анимациях на одно свойство выигрывает последняя в списке,
и запущенные разом они гасят друг друга. Грани въезжают волной по диагонали,
потом медленно дышат вразнобой. Дыхание стартует **после** въезда: при двух
анимациях на одно свойство выигрывает последняя в списке, и запущенные разом
они гасят друг друга.

### Фотографии

В блоке «в цифрах» стоят три фирменных снимка: инженер с планшетом,
команда за чертежами и каска с маркировкой.
Все квадратные 1254×1254 — садятся в ячейку сетки без кадрирования.
Фон первого экрана — `hero-stroyka.jpg`, оттуда же.

**Исходники в PNG лежат в `images/`, готовые JPEG — в `src/assets/`.**
После замены исходника:

```bash
npm run images
```

Снимки лежат в `src/assets`, а не в `public`, и импортируются статически.
Next подмешивает в имя файла хеш содержимого
(`inzhener-planshet.3jxl_35kefdgf.jpg`), поэтому после замены адрес меняется
сам и старый кадр физически не может прилететь из кэша.

Это важнее, чем кажется. В `public` картинка лежит по постоянному адресу,
а `<Image>` запрашивает её в нескольких размерах (`w=256`, `w=384`, `w=640`).
Браузер держит каждый вариант отдельно, и после подмены файла получается
мешанина: один размер свежий, другой из кэша. На экране это выглядит так,
что картинка на секунду становится новой, а потом возвращается старая —
браузер пересчитал `srcset` и взял другой вариант.

Класть PNG под именем `.jpg` тоже нельзя: Next отдаст файл как есть,
без пережатия, и страница потяжелеет в несколько раз.

Кадры к объектам в блоке «Где работала команда» лежат в `src/assets/projects/`
и названы по `id` объекта из `src/content/projects.ts`. Там же
`src/assets/gory.jpg` — пейзаж в угол секции, он уходит под фейд, поэтому
важен не сюжет, а общий тон.

Сам блок — горизонтальная гармошка, `components/ProjectsAccordion.tsx`.
Один объект раскрыт крупно, остальные стоят сложенной гармошкой справа.
До этого была сетка из восьми одинаковых плиток: снимки в ней выходили
мелкими, и опыт читался как список. Там же описано, почему кадр гасится
скримом, а не приглушением — снимки сплошь светлые, и на белом фоне
им не от чего оттолкнуться.

Ширины карточек считает `flex-grow`, поэтому раскрытие одной само сжимает
остальные. На узких экранах гармошка заменена лентой с прокруткой: восемь
вертикальных полосок на телефоне нечитаемы.

**Эти кадры — отрисовки, а не съёмка объектов.** Снимков самих площадок
у компании нет. На части кадров видны чужие товарные знаки, и это тот же
риск, что описан в пункте 3 списка блокеров: изображение чужого объекта
с его логотипом на странице, которую открывает служба безопасности, работает
против доверия сильнее, чем отсутствие картинки. Решение оставлено
за заказчиком, но перед запуском к нему стоит вернуться.

Кадры сжимаются до 1100 точек по ширине — третье поле в паре внутри
`scripts/prepare-images.sh`. В карточке кадр виден примерно на 460 точек;
без обрезки восемь кадров весят пять мегабайт и целиком уезжают на превью
GitHub Pages, где сборка статическая и оптимизатора картинок нет.

Папка `images_for/` — то, что прислал заказчик пачкой. Из двенадцати кадров
в дело пошли восемь; остальные не удалял, но и в сборку они не попадают.

Кадры этапов лежат в `src/assets/stages/` и названы по слагу услуги
(`proektirovanie.jpg`), а привязаны через поле `image` в `src/content/services.ts`.
Привязка идёт по модулю, а не по порядку в списке: услуги можно переставлять
местами, кадры поедут за ними.

Сканы грамот лежат там же, в `src/assets/awards/`, и тоже импортируются.
Сначала они стояли в `public` по прямому адресу — казалось, что им это
безвредно, ведь они не меняются. На превью сканы отдавали 404: сайт там стоит
в подпапке `/<repo>`, а `next/image` в режиме `unoptimized` отдаёт строковый
`src` как есть, без префикса базового пути. При импорте адрес собирает
сборщик, и он верен в любом размещении.

Отсюда правило: **картинки из кода подключаются импортом, а не строкой.**
В `public` остаётся только то, что запрашивают снаружи, — картинка превью
для ссылок.

Чего не хватает: съёмок самих объектов — под раздел «Объекты», под фон
первого экрана и под плитки этапов в `SystemTabs` (там пока тоже грани).

Сканы грамот никуда не делись — они на странице «О компании» в разделе
«Награды», где им и место как документам.
